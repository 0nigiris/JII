# JII UI strings — English (source of truth + fallback).
#
# Keys are namespaced tables; the code references them by dotted path
# (e.g. `install.searching`). Placeholders are `{name}` and are filled by the
# call site via `t!("key", name = value)`. Every key here MUST also exist in
# every other locale (a test enforces parity) — English is what other files
# are translated from.

[common]
aborted = "Aborted."
steps = "steps:"
not_installed = "Not installed: {names}"
no_sources_enabled = "No installation sources are enabled."
no_usable_source = "No usable installation source found on this system."
dry_run_unchanged = "(dry-run: nothing was changed)"
dry_run_not_installed = "(dry-run: nothing was installed)"
dry_run_not_updated = "(dry-run: nothing was updated)"
dry_run_not_removed = "(dry-run: nothing was removed)"
needs_sudo = "  [needs sudo]"
usage_hint = "Usage: jii <package…>  (try `jii --help`)"
unknown = "unknown"
note = "note: {note}"

[list]
empty = "Nothing installed via jii yet."
col_name = "NAME"
col_source = "SOURCE"
col_version = "VERSION"
col_trust = "TRUST"
col_verified = "VERIFIED"
col_status = "STATUS"
status_ok = "ok"

[action]
installed = "installed"
removed = "removed"
updated = "updated"

[audit]
need_attention = "{flagged} of {total} need attention."
all_fine = "All {total} install(s) look fine."

[history]
empty = "No history yet."
col_when = "WHEN"
col_action = "ACTION"
col_package = "PACKAGE"
col_source = "SOURCE"

[how]
no_record = "'{package}' was not installed by jii (no record). Try `jii {package}`."
installed_on = "Installed via {source} on {date}"
version_line = "Version {version}"
trust_line = "Source trust: {trust}"
run = "run: {cmd}"

[health]
healthy = "healthy"
slow = "slow"
offline = "offline"
rate_limited = "rate-limited"

[remove]
continue_rest = "Continue removing the rest?"
cannot_plan = "✗ {name}: cannot plan removal ({reason})"
nothing = "Nothing to remove."
removed = "Removed {names}."
prompt_one = "Remove {name}?"
prompt_many = "Remove {count} packages?"

[update]
none = "No updatable packages."
cannot_plan = "✗ {name}: cannot plan update ({reason})"
all_up_to_date = "All {count} package(s) already up to date."
some_up_to_date = "({count} already up to date)"
updated = "Updated {names}."
prompt_one = "Update {name}?"
prompt_many = "Update {count} packages?"
nothing_to_update = "Nothing to update."
sys_via = "System update via: {sources}"
tracked_up_to_date = "({count} tracked package(s) already up to date)"
prompt_system = "Update your system now?"
complete = "System update complete."
sum_none = "nothing to update"
sum_done = "updated"
sum_changed = "{count} packages updated"
sum_deprecated = "{count} deprecation warning(s)"
sum_eol = "{count} end-of-life runtime(s) — run `flatpak uninstall --unused` to clean up"

[install]
searching = "Searching…"
searching_for = "Searching for '{name}'..."
not_found = "Not found: {names}"
not_found_via = "Not found via source '{source}': {names}"
no_exact_match = "No exact match for '{name}'. Closest: {closest}."
web_url = "Have a look first: {url}"
already_installed = "{name} already installed via {source}{version}"
already_installed_outdated = "{name} is already installed via {source} ({current}). Available: {available}."
update_now = "Update now?"
keeping = "Keeping the installed version."
choose_header = "'{name}' is available from several sources — you choose:"
recommended_tag = "recommended"
continue_rest = "Continue installing the rest?"
installed = "Installed {names}."
also_available = "Also available:"
and_more = "…and {count} more"
summary = "Summary:"
preview = "Install {name}{version} via {source}{why}{sudo}"
gh_searching = "Searching GitHub for '{name}'…"
gh_picker_header = "GitHub repositories matching '{name}' — pick one to install:"
gh_show_more = "↓  Show more results"
gh_no_release = "{slug} publishes no installable Linux binary — pick another."
gh_none = "No GitHub repositories found for '{name}'."
gh_corrected = "Nothing for '{name}' — showing results for '{fixed}' instead."

[doctor]
all_good = "Nothing to set up — you're all good."
config_line = "Config file: {path}"
config_absent = "not created yet — built-in defaults in use"
sources_header = "Sources:"
checks_header = "System checks:"
all_looks_good = "Everything looks good."
things_to_look = "Some items above need a look ({count})."
run_interactive = "Run `jii doctor` in a terminal to set these up interactively."
setup_intro = "Let's set up what's missing — Enter to accept, n to skip:"
q_install = "Install {pkg}?"
q_add_path = "Add {dir} to your PATH?"
q_fix = "{label} — fix it now?"
q_setup = "Set up {title}?"
prereq = "{title} is required first — enabling it:"
would_run = "would run:  {cmd}"
runs = "runs:  {cmd}"
done = "Done."
failed = "Failed: {error}"
no_home = "Can't resolve your home directory — add it to PATH manually."
would_add = "would add to {file}:  {line}"
already_on_path = "{file} already references {dir} — nothing to do."
couldnt_update = "Couldn't update {file}: {error}"
couldnt_write = "Couldn't write {file}: {error}"
added_to_path = "Added to {file}. Run `source {file}` or open a new terminal."
suggestions_header = "Suggestions for your system:"
suggestions_info = "Informational — run `jii doctor` in a terminal to set these up."

[selfupdate]
checking = "Checking for a newer JII…"
check_failed = "Couldn't check for updates: {error}"
up_to_date = "JII is already up to date ({version})."
available = "JII {current} → {latest} available."
update_now = "Update JII now?"
updated = "JII updated to {version}."
removes = "This removes JII itself."
remove_prompt = "Remove JII?"
removed = "JII removed. Thanks for trying it — reinstall anytime."
plan_update = "Update JII {current} → {latest}"
plan_replace = "Replace {exe} in place (no root)"
plan_update_pkg = "Update JII {current} → {latest} via the system package manager"
plan_remove = "Remove JII ({exe})"
plan_remove_pkg = "Remove JII via the system package manager"

[sources]
active = "Active sources:"
unavailable = "Enabled but unavailable (tool not installed):"
hidden = "{count} native manager(s) for other systems hidden — `jii sources --all` to show."
unknown = "Unknown source '{id}'."
known = "Known sources: {list}"
enabled = "Enabled source '{id}'."
disabled_ok = "Disabled source '{id}' — JII will no longer consider it."
already_enabled = "Source '{id}' is already enabled."
already_disabled = "Source '{id}' is already disabled."
add_hint2 = "[add: jii sources add {id}]"
remove_hint = "[remove: jii sources remove {id}]"
remove_system = "'{id}' is a system package manager — JII won't uninstall it (that would break your OS)."
remove_not_installed = "{label} isn't installed — nothing to remove."
remove_script = "{label} was installed by its own script, so JII won't remove it — use its own uninstaller."
remove_no_system_mgr = "No system package manager was found to uninstall {label} with."
remove_unknown_pkg = "Couldn't tell which package provides {label} here (tried: {names}) — remove it with your package manager."
remove_confirm = "This will remove {label} from your system:"
remove_prompt = "Remove it?"
removed = "Removed {label}."
remove_failed = "Could not remove {label}: {error}"

[cache]
path = "Search cache: {path}"
absent = "empty — nothing cached yet"
no_path = "No cache location could be resolved on this system."
cleared = "Cleared the search cache ({path})."
already_empty = "The search cache is already empty."
clear_failed = "Could not clear the cache: {error}"

[bootstrap]
needed = "{name} is available via {label}, which isn't installed yet."
confirm = "Install {label} and get {name} through it?"
declined = "Skipped — {name} can't be installed without {label}."

[providers]
installed = "Installed ecosystems:"
available = "Available to install:"
add_hint = "{label} — jii providers add {id}"
unknown = "Unknown ecosystem: {name}"
known = "Known ecosystems: {names}"
already_installed = "{label} is already installed — it's a package manager JII drives."
looking = "Looking for a package that provides {label}…"
not_found = "Couldn't find a package for {label} in your active sources."
tried = "Tried: {names}"
script_only = "{label} isn't in your distro's repositories — it installs via its own script."
script_wont_run = "JII won't run an installer script for you. To install it, run:"

[aur]
no_helper = "No AUR helper (yay/paru) is installed."
not_arch = "{helper} is an AUR helper — it only applies to Arch-based systems."
helper_present = "{helper} is already installed."
helper_intro = "{helper} is built from the AUR with makepkg. JII won't run makepkg for you; to install {helper}, run:"

[prompt]
menu_hint = "↑/↓ move · Enter select · click to pick · Esc cancel"
install_one = "Install?"
install_all = "Install all?"
less_trusted = "This install includes a less-trusted source ({level}) — explicit confirmation required."

[plan]
title = "Plan: {name} (via {source})"
download = "download: {size} bytes"
privileges = "privileges: {level}"
priv_root = "root required"
priv_user = "user"
actions_none = "actions: (none)"
actions = "actions:"
act_download = "download {url} → {dest} [{verify}]"
act_place = "place → {dest} (mode {mode})"
act_extract = "extract {member} from {archive} → {dest} (mode {mode})"
act_remove = "remove {path}"
act_replace = "replace {dest} ← {src}"
priv_root_shown = "privileges: root required (the exact command is shown above)"

[setup]
welcome = "Welcome to JII 👋"
intro1 = "JII installs Linux software for you: it searches the sources you already"
intro2 = "have (dnf, Flatpak, …), picks the best one, and tells you why."
confirm_spend = "Spend 30 seconds setting it up?"
declined = "No problem — try `jii firefox` to install something, or `jii setup` anytime."
couldnt_save = "Could not save settings: {error}"
detail_q = "How much should JII tell you?"
detail_friendly = "Friendly — short, clear output (recommended)"
detail_advanced = "Advanced — full detail, source rationale, diagnostics"
run_doctor_q = "Run a quick system check and setup (jii doctor) now?"
complete = "Setup complete."
first_use = "Looks like this is your first time using JII — a quick setup first."
will_run_after = "After the (optional) setup, this will run:  {cmd}"
now_running = "▶ Now running:  {cmd}"
gh_header = "GitHub access token (optional, but recommended)"
gh_benefit = "JII installs release binaries from GitHub and searches repos there. Without a token GitHub limits anonymous requests to about 60 per hour — enough to hit a wall mid-install; a token raises that to 5000/hour, so search and install stay fast and reliable."
gh_already = "{env} is already set — you're all set, nothing to do."
gh_step_create = "1. Create a token → https://github.com/settings/tokens  ('Generate new token (classic)'). No scopes are needed to read public repositories — leave every box unchecked."
gh_step_export = "2. Add it to your shell profile (~/.bashrc, or ~/.zshrc for zsh):"
gh_step_reload = "3. Open a new terminal (or run `source ~/.bashrc`). Verify any time with `jii doctor`."
gh_never = "JII only reads this token to talk to GitHub; it is never stored, logged, or sent anywhere else."

[parse]
invalid = "Invalid package '{raw}': {reason}"
unknown_source = "Unknown source '{source}' in '{name}:{source}'. Known sources: {known}."
pin_unsupported = "Version/channel pinning (@{pin}) isn't supported yet — it's coming with the version chooser. Use '{name}' without the '@' part for now."

[trust]
official = "official"
community = "community"
untrusted = "untrusted"

[search]
none = "No candidates found for '{name}'."
header = "Candidates for '{name}' (best first):"

[info]
none_found = "No information found for '{name}' in your enabled sources."
row_source = "Source"
row_version = "Version"
row_license = "License"
row_homepage = "Homepage"
row_repository = "Repository"
row_author = "Author"
available_from = "Available from {count} source(s):"
recommended = "Recommended: {source}"

[facts]
signed = "signature/checksum verifiable"
version = "version {version}"
arch_mismatch = "⚠ may not match this architecture"
trust_source = "{trust} source"

[reason]
version = "Version {v}"
repository = "Repository: {repo}"
remove_one = "Remove {name} (installed via {mgr})"
remove_many = "Remove (via {mgr}): {names}"
update_one = "Update {name} via {mgr}"
update_many = "Update (via {mgr}): {names}"
update_one_reinstall = "Update {name} via {mgr} (reinstall newest)"
update_many_reinstall = "Update (via {mgr}, reinstall newest): {names}"
upgrade_all_system = "Upgrade all system packages (via {mgr})"
dnf_official = "Official Fedora package"
dnf_native = "Native — installs and updates with the system"
dnf_official_many = "Official Fedora packages: {names}"
copr_community = "Community COPR repo — not official Fedora"
copr_enables = "Enables a third-party repository"
copr_repo = "COPR (community repository)"
copr_enables_then = "Enables the repo, then installs {name}"
flatpak_sandboxed = "Sandboxed application"
flatpak_crossdistro = "Cross-distro (Flatpak/Flathub), no root"
flatpak_short = "Flatpak (sandboxed)"
flatpak_remote = "Remote: {remote}"
flatpak_upgrade_all = "Update all Flatpak apps and runtimes"
cargo_source = "Built from crates.io source"
cargo_user = "User install (~/.cargo/bin, no root)"
cargo_registry = "crates.io (Rust community registry)"
cargo_registry_many = "crates.io (Rust community registry): {names}"
cargo_builds = "Builds {name} into ~/.cargo/bin (no root; ensure it is on PATH)"
aur = "AUR (built from source, via {helper})"
aur_upgrade_all = "Update all AUR packages (via {helper})"
npm_registry = "npm registry (community)"
npm_registry_many = "npm registry (community): {names}"
npm_installs = "Installs {name} into {prefix}/bin (no root; ensure it is on PATH)"
npm_upgrade_all = "Update all global npm packages"
npm_remove_label = "Remove (via npm)"
npm_update_label = "Update (via npm, reinstall newest)"
pipx_pypi = "PyPI (Python application, via pipx)"
pipx_installs = "Installs {name} into ~/.local/bin (no root; ensure it is on PATH)"
pipx_upgrade_all = "Upgrade all pipx apps"
go_module = "Go module (community)"
go_builds = "Builds {name} into {dir} (no root; ensure it is on PATH)"
go_modules_many = "Go modules (community): {names}"
go_remove = "Remove {name} (delete {path})"
go_update_one = "Update {name} via go install (newest)"
go_update_many = "Update (via go install, newest): {names}"
forge_thirdparty = "Third-party release binary — verify before trusting"
forge_installs_local = "Installs to ~/.local/bin (no root)"
forge_remove = "Remove {name} from {dest}"
forge_release = "Release ({slug})"
forge_extracts = "Extracts '{name}' from the release archive"
forge_verified = "Verified sha256 checksum"
forge_unverified = "⚠ no checksum published — unverified"
forge_installs = "Installs to {dest} (no root)"
apt_system = "System package (apt)"
apt_system_many = "System packages (apt): {names}"
pacman_official = "Official Arch package"
pacman_official_many = "Official Arch packages: {names}"
zypper_official = "Official openSUSE package"
zypper_official_many = "Official openSUSE packages: {names}"
zypper_upgrade_all = "Update all system packages (via zypper)"
void_official = "Official Void package"
void_official_many = "Official Void packages: {names}"
gentoo_official = "Official Gentoo package (built from source)"
gentoo_official_many = "Official Gentoo packages (built from source): {names}"
brew_formula = "Homebrew (community formula)"
brew_installs = "Installs into Homebrew's prefix (no root)"
brew_formula_many = "Homebrew formulae (community): {names}"
brew_upgrade_all = "Upgrade all Homebrew formulae"
snap_store = "Snap Store (community)"
snap_classic = "Classic confinement — full system access (--classic)"
snap_store_many = "Snap Store (community): {names}"
snap_refresh_all = "Refresh all snaps"
nix_pkg = "Nixpkgs package (community)"
nix_installs = "Installs into your Nix profile (no root)"
nix_pkg_many = "Nixpkgs packages (community): {names}"
nix_remove = "Remove {name} from your Nix profile"
nix_remove_many = "Remove from Nix profile: {names}"
nix_upgrade_one = "Upgrade {name} in your Nix profile"
nix_upgrade_many = "Upgrade in Nix profile: {names}"
nix_upgrade_all = "Upgrade all packages in your Nix profile"

[remedy]
unknown_source = "'{id}' is not a source JII knows. Known sources: {known}. Fix the `priority`/`disabled` list in your config (or run `jii setup`)."
config = "Check the config file (usually ~/.config/jii/config.toml), or run `jii setup` to regenerate it."
io_not_found = "{path} does not exist."
io_denied = "Permission denied for {path}. Check its ownership and permissions."

[library]
cargo = "'{name}' is a Rust library on crates.io — it ships no executable, so there's nothing to install as a program."
npm = "'{name}' is an npm library — a code dependency, not a runnable program. JII installs programs, so there's nothing to install; to use it in a project run `npm install {name}`."

[check]
internet_ok = "Internet is reachable"
internet_missing = "No internet connection"
internet_advice = "Most sources (GitHub, Flatpak, cargo, npm…) need the network. Check your connection or proxy settings."
git_ok = "git is installed"
git_missing = "git is not installed"
git_advice = "Some installs (cargo git dependencies, source builds) need it. Add it with:  jii git"
curl_ok = "curl is installed"
curl_missing = "curl is not installed"
curl_advice = "Handy for scripts and manual downloads. Add it with:  jii curl"
path_ok = "{dir} is on your PATH"
path_missing = "{dir} is not on your PATH"
local_path_advice = "User-space installs (cargo, npm, pipx, go, GitHub binaries) land there, so their commands won't be found."
cargo_path_advice = "Cargo installs binaries there, so their commands won't be found."
flathub_ok = "Flathub remote is configured"
flathub_missing = "Flatpak is installed but the Flathub remote is missing"
flathub_advice = "Most Flatpak apps live on Flathub. Add it with:  flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo"
token_ok = "{env} is set — GitHub requests aren't rate-limited"
token_missing = "{env} is not set — GitHub is limited to ~60 requests/hour"
token_advice = "Optional: export {env}=<a GitHub token> to lift the anonymous rate limit."

[nix]
strategy_header = "How do you want to install {name} with Nix?"
strategy_imperative = "Install now — nix profile install (imperative)"
strategy_imperative_hint = "Immediate; not recorded in your Nix configuration."
strategy_add_to = "Add to {file}"
strategy_hint_system = "Declarative (NixOS) — applied on `sudo nixos-rebuild switch`."
strategy_hint_home = "Declarative (home-manager) — applied on `home-manager switch`."
guidance_intro = "Add {name} to {attr} in {file}:"
guidance_apply = "Then apply the change:"
guidance_backup = "Tip: back the file up first — it's your own hand-written config, and JII won't touch it."
guidance_footer = "Nothing was installed — JII only shows the change. Add the snippet and apply it yourself when ready."
edit_intro = "This is the change JII will make to {file}:"
edit_confirm = "Write this change?"
edit_written = "Updated {file}."
edit_backup = "A backup was saved to {backup}."
edit_apply = "Apply it when ready:  {cmd}"
edit_failed = "Could not edit the file: {error}"
edit_dry_run = "Dry run — the file was not changed."
edit_root_cmds = "This config is root-owned; JII will run these as root (via sudo/pkexec):"

[lang]
current = "Current language: {lang}"
available = "Available: {list}"
set = "Language set to {lang}."
unknown = "Unknown language '{code}'. Available: {list}"
